name: CD - Admin UI
on:
  workflow_dispatch:
   inputs:
     environment:
       description: "Deploy environment"
       required: true
       default: "dev"
     tag:
       description: "Docker image tag"
       required: true
    
env:
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ env.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Pull image
        run: |
          docker pull $DOCKERHUB_USERNAME/admin-ui:${{ github.event.inputs.tag }}

      - name: Deploy
        run: |
          echo "Deploying Admin UI"
          # Add the command
